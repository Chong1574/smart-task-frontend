{
  "name": "Bazar - MakerWorld Scraper",
  "nodes": [
    {
      "parameters": { "rule": { "interval": [{ "field": "cronExpression", "expression": "0 3 * * *" }] } },
      "id": "cron-daily",
      "name": "Cron 03:00 daily",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.1,
      "position": [240, 300]
    },
    {
      "parameters": {
        "jsCode": "// Emite 3 items, uno por offset. El HTTP node siguiente hara 1 request por item.\nreturn [0, 40, 80].map(offset => ({ json: { offset } }));"
      },
      "id": "offsets",
      "name": "Emit offsets",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [340, 300]
    },
    {
      "parameters": {
        "url": "https://makerworld.com/api/v1/search-service/select/design2",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            { "name": "orderBy", "value": "score" },
            { "name": "designType", "value": "0" },
            { "name": "isFromSearchList", "value": "false" },
            { "name": "limit", "value": "40" },
            { "name": "offset", "value": "={{ $json.offset }}" }
          ]
        },
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "User-Agent", "value": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36" },
            { "name": "Accept", "value": "application/json, text/plain, */*" },
            { "name": "Accept-Language", "value": "es-ES,es;q=0.9,en;q=0.8" },
            { "name": "Referer", "value": "https://makerworld.com/es/search/models" },
            { "name": "Origin", "value": "https://makerworld.com" }
          ]
        },
        "options": {
          "timeout": 30000,
          "response": { "response": { "responseFormat": "json" } }
        }
      },
      "id": "http-search",
      "name": "MakerWorld: search",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [500, 300]
    },
    {
      "parameters": {
        "jsCode": "// Aplana los hits de los 3 responses en items individuales.\nreturn items.flatMap(it => (it.json?.hits || []).map(hit => ({ json: hit })));"
      },
      "id": "flatten",
      "name": "Flatten hits",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [660, 300]
    },
    {
      "parameters": {
        "jsCode": "// Normaliza + sanitiza títulos. Sale con externalId, licenseType mapeado, imageUrl, tags, sourceUrl.\nconst LICENSE_MAP = {\n  'BY': 'CC-BY', 'BY-SA': 'CC-BY-SA', 'BY-NC': 'CC-BY-NC', 'BY-NC-SA': 'CC-BY-NC',\n  'BY-ND': 'CC-BY', 'BY-NC-ND': 'CC-BY-NC', 'CC0': 'CC0',\n  'Standard Digital File License': 'commercial',\n};\nconst PATTERNS = [\n  /\\|?\\s*(no |sin )?(ams|supports?|soportes?)\\b/gi,\n  /\\|?\\s*easy\\s*(to\\s*)?print(ing)?\\b/gi,\n  /\\|?\\s*no\\s*supports?\\b/gi,\n  /\\|?\\s*免支撑\\S*/g,\n  /\\|?\\s*多色一盘\\S*/g,\n  /\\|?\\s*参数优化\\S*/g,\n  /\\|?\\s*模型优化\\S*/g,\n  /\\|?\\s*\\d+\\.?\\d*mm\\s*layer\\b.*$/i,\n  /\\|?\\s*\\d+\\s*walls?\\b.*$/i,\n  /\\|?\\s*\\d+%\\s*infill\\b.*$/i,\n];\nfunction cleanTitle(t) {\n  if (!t) return 'Sin título';\n  let out = String(t);\n  for (const p of PATTERNS) out = out.replace(p, '');\n  out = out.replace(/\\s*[|·—–-]\\s*$/g, '').replace(/\\s{2,}/g, ' ').trim();\n  return out.length >= 3 ? out : 'Sin título';\n}\nreturn items.map(({ json: d }) => {\n  const rawLicense = (d.license || '').trim();\n  const licenseType = LICENSE_MAP[rawLicense] || 'unknown';\n  const author = d.designCreator?.name || 'Unknown';\n  const externalId = String(d.id ?? '');\n  const slug = d.slug || externalId;\n  return {\n    json: {\n      externalId,\n      source: 'MAKERWORLD',\n      sourceUrl: `https://makerworld.com/en/models/${slug}`,\n      title: cleanTitle(d.titleTranslated || d.title),\n      description: '',\n      imageUrl: d.cover || d.coverPortrait || null,\n      licenseType,\n      licenseAttribution: `${author} — via MakerWorld`,\n      category: (Array.isArray(d.tags) && d.tags[0]) || null,\n      tags: Array.isArray(d.tags) ? d.tags.slice(0, 15) : [],\n      trendScore: Math.round(d.hotScore ?? d.likeCount ?? 0),\n      provider: 'LOCAL',\n    },\n  };\n});"
      },
      "id": "normalize",
      "name": "Normalize + sanitize",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [820, 300]
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "loose" },
          "conditions": [
            { "id": "lic", "leftValue": "={{ $json.licenseType }}", "rightValue": "unknown", "operator": { "type": "string", "operation": "notEquals" } },
            { "id": "id",  "leftValue": "={{ $json.externalId }}",  "rightValue": "",        "operator": { "type": "string", "operation": "notEmpty" } }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "filter",
      "name": "Drop unknown licenses",
      "type": "n8n-nodes-base.filter",
      "typeVersion": 2,
      "position": [980, 300]
    },
    {
      "parameters": {
        "url": "=https://makerworld.com/api/v1/design-service/design/{{ $json.externalId }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "User-Agent", "value": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36" },
            { "name": "Accept", "value": "application/json, text/plain, */*" },
            { "name": "Accept-Language", "value": "es-ES,es;q=0.9,en;q=0.8" },
            { "name": "Referer", "value": "https://makerworld.com/es/search/models" }
          ]
        },
        "options": {
          "timeout": 20000,
          "response": { "response": { "responseFormat": "json" } },
          "batching": { "batch": { "batchSize": 5, "batchInterval": 500 } }
        }
      },
      "id": "http-detail",
      "name": "MakerWorld: fetch detail",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1120, 300],
      "notes": "Un request por producto. batch 5x500ms para evitar rate limit CF."
    },
    {
      "parameters": {
        "jsCode": "// Suma TODOS los plates de cada instance → variante. Sanitiza nombre de variante. priceFrom = min.\nconst FILAMENT_PRICE_PER_KG = 400, WASTE_PCT = 5, KWH_PRICE = 4.5, WATTS = 100, MARGIN_PCT = 300;\nconst VARIANT_PATTERNS = [\n  /\\|?\\s*(no |sin )?(ams|supports?|soportes?)\\b/gi,\n  /\\|?\\s*\\d+\\.?\\d*mm\\s*layer\\b.*$/i,\n  /\\|?\\s*\\d+\\s*walls?\\b.*$/i,\n  /\\|?\\s*\\d+%\\s*infill\\b.*$/i,\n  /\\|?\\s*免支撑\\S*/g,\n  /\\|?\\s*多色一盘\\S*/g,\n  /\\|?\\s*参数优化\\S*/g,\n  /\\|?\\s*模型优化\\S*/g,\n];\nfunction cleanVariantName(t, idx) {\n  if (!t) return `Variante ${idx + 1}`;\n  let out = String(t);\n  for (const p of VARIANT_PATTERNS) out = out.replace(p, '');\n  out = out.replace(/\\s*[|·—–-]\\s*$/g, '').replace(/\\s{2,}/g, ' ').trim();\n  return out.length >= 3 ? out : `Variante ${idx + 1}`;\n}\nfunction priceForPlates(plates) {\n  const grams = plates.reduce((s, p) => s + (Number(p.weight) || 0), 0);\n  const seconds = plates.reduce((s, p) => s + (Number(p.prediction) || 0), 0);\n  const hours = seconds / 3600;\n  if (grams <= 0 || hours <= 0) return null;\n  const effectiveGrams = grams * (1 + WASTE_PCT / 100);\n  const filament = (effectiveGrams / 1000) * FILAMENT_PRICE_PER_KG;\n  const electricity = hours * (WATTS / 1000) * KWH_PRICE;\n  const subtotal = filament + electricity;\n  return { grams, hours: Number(hours.toFixed(2)), price: Math.round(subtotal * (1 + MARGIN_PCT / 100)) };\n}\nreturn items.map(({ json }) => {\n  const instances = json?.instances || [];\n  const designExtension = json?.designExtension || {};\n  const designPics = Array.isArray(designExtension?.design_pictures) ? designExtension.design_pictures.map(p => p.url).filter(Boolean) : [];\n  const cover = json?.cover;\n  const images = cover ? [cover, ...designPics.filter(u => u !== cover)] : designPics;\n\n  const variantsRaw = instances.map((ins, i) => {\n    const plates = (((ins.extention) || {}).modelInfo || {}).plates || [];\n    const calc = priceForPlates(plates);\n    if (!calc) return null;\n    return { name: cleanVariantName(ins.title || ins.titleTranslated, i), grams: calc.grams, hours: calc.hours, price: calc.price };\n  }).filter(Boolean);\n\n  const variants = variantsRaw.sort((a, b) => a.price - b.price);\n  const priceFrom = variants.length ? variants[0].price : null;\n  const description = (json?.description || '').slice(0, 2000);\n\n  return {\n    json: {\n      __detail: {\n        id: json?.id,\n        images,\n        description,\n        variants,\n        priceFrom,\n      },\n    },\n  };\n});"
      },
      "id": "compute",
      "name": "Compute price + variants",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1340, 300]
    },
    {
      "parameters": {
        "jsCode": "// Merge: cada item aqui trae __detail; buscar el normalized correspondiente por indice (pairing 1:1 con 'Drop unknown licenses').\nconst normalized = $('Drop unknown licenses').all();\nconst products = items.map((item, i) => {\n  const n = normalized[i]?.json || {};\n  const det = item?.json?.__detail || {};\n  return {\n    ...n,\n    descriptionHtml: det.description || '',\n    images: det.images || [],\n    variants: det.variants || [],\n    priceFrom: det.priceFrom ?? null,\n    price: det.priceFrom ?? null,\n  };\n});\nreturn [{ json: { products } }];"
      },
      "id": "wrap",
      "name": "Wrap as { products: [...] }",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1560, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://host.docker.internal:3000/api/products/sync",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ products: $json.products }) }}",
        "options": {
          "timeout": 60000,
          "response": { "response": { "responseFormat": "json" } }
        }
      },
      "id": "post-sync",
      "name": "POST /api/products/sync",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1780, 300],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000,
      "notes": "Auth: seleccionar credencial Header Auth 'Taskman Sync' post-import. Ver README debug si D1 queda vacío."
    }
  ],
  "connections": {
    "Cron 03:00 daily":         { "main": [[{ "node": "Emit offsets",                "type": "main", "index": 0 }]] },
    "Emit offsets":             { "main": [[{ "node": "MakerWorld: search",          "type": "main", "index": 0 }]] },
    "MakerWorld: search":       { "main": [[{ "node": "Flatten hits",                "type": "main", "index": 0 }]] },
    "Flatten hits":             { "main": [[{ "node": "Normalize + sanitize",        "type": "main", "index": 0 }]] },
    "Normalize + sanitize":     { "main": [[{ "node": "Drop unknown licenses",       "type": "main", "index": 0 }]] },
    "Drop unknown licenses":    { "main": [[{ "node": "MakerWorld: fetch detail",    "type": "main", "index": 0 }]] },
    "MakerWorld: fetch detail": { "main": [[{ "node": "Compute price + variants",    "type": "main", "index": 0 }]] },
    "Compute price + variants": { "main": [[{ "node": "Wrap as { products: [...] }", "type": "main", "index": 0 }]] },
    "Wrap as { products: [...] }": { "main": [[{ "node": "POST /api/products/sync", "type": "main", "index": 0 }]] }
  },
  "settings": { "executionOrder": "v1" },
  "active": false
}
